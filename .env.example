# URL base da API Django. Em desenvolvimento local pode ficar vazio
# (o código já usa http://localhost:8000/api como padrão).
# Em produção, aponte para a URL do Web Service publicado no Render.
VITE_API_URL=http://localhost:8000/api
